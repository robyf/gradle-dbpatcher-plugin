apply plugin: 'groovy'

wrapper {
    gradleVersion = '3.1'
}

repositories {
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile group: 'net.robyf', name: 'dbpatcher', version: '1.0.1'
    testCompile 'junit:junit:4.12'
}
